# Work Log - 2026-06-18

Project: `C:\Development\projects\my-game-1`

## Summary

- Made the game selector landing page scroll reliably in mobile browser viewports.
- Committed and pushed the responsive landing fix to `origin/codex/add-puzzle-games`.

## Completed Work

- Added an explicit `is-landing` body state when rendering the selector.
- Changed landing-mode layout so `body`, `#app`, and `#game` can use normal document scrolling instead of the fullscreen hidden-overflow shell.
- Updated the landing surface to use `min-height: 100dvh`, visible overflow, and touch-friendly scrolling.
- Verified that launching Super Star Trek leaves landing mode and returning via Game Select restores it.

## Files Changed

```text
docs/work-log.md
src/main.ts
src/style.css
```

## Verification

- `npm test -- --runInBand` passed: 11 suites, 57 tests.
- `npm run build` passed. Vite still reported the existing large JS chunk warning.
- Browser-checked a 390x844 viewport: the landing page scrolled to the final Super Star Trek card, game launch switched to fullscreen mode, and Game Select restored landing scroll mode.

## Follow-ups

- Consider code-splitting Phaser or game runtimes later to address the existing Vite large chunk warning.

## Git Context

- Repository: yes
- Branch: `codex/add-puzzle-games`

### Diff Stat

```text
(none)
```

### Staged Diff Stat

```text
(none)
```

### Today's Commits

```text
70a82a9 fix: make landing page scroll on mobile
```

## Commit And Push

- Commit: `70a82a9 fix: make landing page scroll on mobile`
- Push: pushed `codex/add-puzzle-games` to `origin/codex/add-puzzle-games`
